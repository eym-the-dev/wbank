package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Brand colors for "Sleek Interface" theme - Overhauled for Premium Corporate Dark Mode
val SleekBackground = Color(0xFF000000)      // Pure AMOLED Black background
val SleekNavyHeader = Color(0xFFFFFFFF)      // Bold crisp white headers
val SleekDigitalBlue = Color(0xFF0B84FF)     // Electric modern active blue
val SleekSecondaryBlue = Color(0xFF1C1C1E)   // Precise subtle charcoal card layering
val SleekSurfaceVariant = Color(0xFF2C2C2E)  // Slate-grey for dividers and accents

// High contrast text colors for premium dark layout
val TextPrimaryDark = Color(0xFFFFFFFF)      // Pure crisp white text
val TextSecondarySlate = Color(0xFF9E9E9E)   // Highly legible light-grey secondary text

// Premium Card Gradient Colors (keeps the exclusive Platinum look)
val PremiumGoldAccent = Color(0xFFFFD700)    // Polished gold accents
val GoldAccent = Color(0xFFE5A93C)           // Rich luxury metallic golden tone
val CharcoalGrayStart = Color(0xFF1E1E20)    // Platinum card gradient start
val CharcoalGrayMid = Color(0xFF2C2C2E)      // Platinum card gradient middle
val CharcoalGrayEnd = Color(0xFF161618)      // Platinum card gradient end

// High fidelity UI Accents (Optimized for Dark mode readability)
val SleekDebitRed = Color(0xFFFF453A)        // Transaction Debit
val SleekDebitRedLight = Color(0xFF2C1414)   // Deep red container background (dark mode)
val DarkRedText = Color(0xFFFFD6D4)          // Readability-adjusted text inside containers
val SleekCreditGreen = Color(0xFF30D158)     // Transaction Credit
val SleekCreditGreenLight = Color(0xFF142C14) // Deep green container background (dark mode)
val DarkGreenText = Color(0xFFD0FFD6)        // Readability-adjusted text inside containers
